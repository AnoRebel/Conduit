{
  "name": "@conduit/examples",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Runnable examples, executed as part of the test suite so they cannot rot.",
  "scripts": {
    "test": "tsc -p tsconfig.json && bun run rooms.ts && bun run topics.ts && bun run cluster.ts && bun run go-parity.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@conduit/client": "workspace:*",
    "@conduit/server": "workspace:*",
    "@conduit/shared": "workspace:*",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "@types/node": "catalog:",
    "@types/ws": "catalog:",
    "typescript": "catalog:"
  }
}
